import React from 'react';
import Button from 'react-bootstrap/Button'
import './button.css';

export default function PackageButton() {
  return (
    <Button className="package_button" variant="primary">Primary</Button>
  );
}
